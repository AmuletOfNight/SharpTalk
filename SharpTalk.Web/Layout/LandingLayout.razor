@inherits LayoutComponentBase

<div class="landing-layout">
    <main>
        @Body
    </main>
</div>

<style>
    .landing-layout {
        display: flex;
        flex-direction: column;
        height: 100vh;
        background: linear-gradient(135deg, #1e1f22 0%, #2b2d31 100%);
        color: #fff;
        overflow: hidden;
    }

    .landing-layout main {
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        background-image: radial-gradient(circle at 50% 50%, rgba(88, 101, 242, 0.05) 0%, transparent 50%);
    }
</style>
